<%= form_for @planning do |f| %>
  <% if @planning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@planning.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @planning.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  
  <%= f.fields_for :periods do |builder| %>
       <%= builder.label :planned_quantity, "Quantidade planejada" %>
       <%= builder.number_field :planned_quantity%>

  <%end%>
  <br>
  <br>
  <div class="actions">
    <%= f.submit "OK", class: "btn btn-success" %>
  </div>
  <br>
<%end%>
