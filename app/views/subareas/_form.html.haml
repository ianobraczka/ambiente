= form_for(@subarea) do |f|
  - if @subarea.errors.any?
    #error_explanation
      %h2
        = pluralize(@subarea.errors.count, "error")
        prohibited this product from being saved:
      %ul
        - @subarea.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label "Nome"
    = f.text_field :name
  %br/
  .actions
    = f.submit "OK"
